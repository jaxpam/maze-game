

export default class NewGameService {

    public createNewGame() {
        // dispatch newPlayer to the game store
        // get initialMaze from the store
        // clone the Maze => JSON.parse(JSON.stringify(initialMaze))
    }

    // public completeGame() {
    //     // store player in local storage
    // }
}
